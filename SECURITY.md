# 安全说明 / Security Policy

## 信任模型（请先读这一节）

**subagg 的数据库里存放的是你的代理凭据。** UUID、密码、Reality 私钥短 ID、上游订阅
URL——这些一旦泄露，等同于把你的所有代理账号交给对方。请按照"存放密钥的服务"来对待它，
而不是按照"一个自用小工具"。

本项目的设计前提是：

- **单用户自托管。** 没有多用户体系，没有 RBAC。持有 `ADMIN_TOKEN` 的人拥有全部权限。
- **管理端不应暴露公网。** 默认监听 `127.0.0.1`。
- **`/sub/:token` 端点必须暴露**（否则客户端拉不到订阅），但它只按 token 返回对应 profile
  的内容，且 token 可单独吊销。

## 部署要求

1. **必须启用 HTTPS。** 订阅内容以明文传输时，中间人可以直接拿走全部节点凭据。
   在 subagg 前面挂 Caddy / Nginx 反代并配好证书。Caddy 最省事：

   ```caddyfile
   sub.example.com {
       reverse_proxy 127.0.0.1:8787
   }
   ```

2. **不要把 `HOST` 改成 `0.0.0.0` 后直接开放端口。** 如果必须直连，用防火墙限制来源 IP。

3. **`ADMIN_TOKEN` 用随机值，不要用弱口令。**

   ```bash
   openssl rand -hex 32
   ```

   服务在 `ADMIN_TOKEN` 缺失或短于 16 字符时**拒绝启动**——本项目不提供默认口令，
   也不允许"先跑起来再说"。

4. **保护 `.env` 与数据库文件权限。**

   ```bash
   chmod 600 .env
   ```

   数据库文件由程序创建为 `0600`。

5. **考虑把管理界面限制在内网或 VPN 后。** 反代层加一条来源 IP 白名单，或干脆只通过
   SSH 隧道访问：

   ```bash
   ssh -L 8787:127.0.0.1:8787 user@your-vps
   ```

## 项目内置的防护措施

| 措施 | 说明 |
|------|------|
| 管理 API 鉴权 | `/api/*` 全量要求 `Authorization: Bearer <ADMIN_TOKEN>`，比较使用**时间恒定**算法，避免计时侧信道 |
| 订阅 token | 32 字节密码学随机，URL-safe base64，可单独吊销与轮换 |
| 日志脱敏 | 订阅 URL、UUID、密码、token 在写日志前一律打码，不会出现在 journalctl 里 |
| IP 不存明文 | 访问日志中的 IP 以 `HMAC-SHA256(salt, ip)` 存储，仅用于区分不同来源 |
| 限流 | `/sub/:token` 默认 60 req/min/IP |
| 抓取防护 | 上游响应有大小上限（默认 8 MiB）与超时，防止被恶意/异常的上游拖垮 |
| 正则防护 | 用户提供的过滤正则限制长度并拒绝明显的灾难性回溯模式（见下） |
| 数据库文件权限 | 创建时设为 `0600` |

### 关于正则表达式（ReDoS）

过滤规则允许用户填写正则。JavaScript 的正则引擎存在灾难性回溯风险，一条
`(a+)+$` 这样的模式配上特定输入可以让 CPU 跑满。

本项目的处理：限制模式长度（≤ 200 字符）、编译期 `try/catch`、并拒绝明显的嵌套量词结构。
这是**缓解而非根治**——完备的方案需要 RE2 这类线性时间引擎。

由于信任模型是单用户自托管（写规则的人就是部署的人），我们认为当前的缓解措施与风险
是匹配的。**如果你打算把管理界面开放给不完全信任的人使用，这个假设就不成立了**，
请不要这么做。

## 已知的能力边界（不是 bug）

- **无法精确计量好友的流量。** 好友的代理流量直连代理服务器，不经过 subagg。我们只能
  记录订阅链接的**拉取行为**（时间、次数、客户端类型），无法知道对方用掉了多少 GB。
  界面上呈现的一切都基于真实采集的数据，不做编造的估算。
- **无法阻止好友转发你的订阅链接。** token 可以吊销和轮换，但在吊销之前，拿到链接的人
  都能拉取。共享给别人时请有这个心理准备。

## 报告漏洞

请**不要**通过公开 issue 报告安全漏洞。

发送邮件至 `security@example.com`（请替换为你 fork 后的实际联系方式），包含：

- 漏洞描述与影响范围
- 复现步骤
- 你认为的修复方向（如果有）

我们会在 72 小时内确认收到，并在修复发布后于 CHANGELOG 中致谢（除非你要求匿名）。

## 支持的版本

项目处于 `0.x` 阶段，仅对最新的 minor 版本提供安全修复。
